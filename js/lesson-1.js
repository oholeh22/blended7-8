console.log(`Hello ${}`);
console.log();
console.log('object :>> ', object);
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}
switch (key) {
    case value:
        
        break;

    default:
        break;
}

if (condition) {
    
}